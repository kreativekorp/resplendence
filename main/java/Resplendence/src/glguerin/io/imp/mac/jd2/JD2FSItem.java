/*** Copyright 1998, 1999, 2001 by Gregory L. Guerin.** Terms of use:**  - Briefly: OPEN SOURCE under Artistic License -- credit fairly, use freely, alter carefully.**  - Fully: <http://www.amug.org/~glguerin/sw/artistic-license.html>** This file is from the MacBinary Toolkit for Java:**   <http://www.amug.org/~glguerin/sw/#macbinary> */package glguerin.io.imp.mac.jd2;import glguerin.io.*;import glguerin.io.imp.mac.*;// --- Revision History ---// 18Jun01 GLG  create stub subclass// 17Jan2003 GLG  add mayResolve() that always returns F/**** An FSSpecItem is an FSItem that uses the FSSpec-based API under** Classic Mac OS.  It will not work as-is on FSSpec-based Mac OS X, since** the pathname semantics are different on Mac OS X.**** @author Gregory Guerin*/public class JD2FSItem  extends FSSpecItem{	/**	** Construct an empty FSSpecItem.	*/	public	JD2FSItem()	{  super();  }	/**	** Concrete factory-method.	*/	protected ForkRW	newFork( boolean forWriting, int forkRefNum, String tag )	{  return ( new JD2Fork( forWriting, forkRefNum, tag ) );  }	/** Make a concrete FSSpec. */	protected FSSpec	newFSSpec()	{  return ( new JD2FSSpec() );  }	/** Make a concrete CatalogAccess. */	protected CatalogAccess	newCatalogAccess()	{  return ( new JD2Cat() );  }	/** Make a concrete CommentAccess. */	protected CommentAccess	newCommentAccess()	{  return ( new JD2Comment() );  }	/**	** Called by refItem().	** On entry, myCatInfo references an "apparent" directory, symlink, or alias-file.	** Return T if caller should attempt to resolve it, F if not.	**<p>	** Always returns F.  Non-leaf aliases are never resolved on-the-fly.	*/	protected boolean	mayResolve()	{  return ( false );  }}